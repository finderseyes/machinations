# Modifier tests
---
nodes:
  - type: source
    id: p0
    connections:
      - --> p1
      - --> p2 :e_1_2

  - type: pool
    id: p1
    activationMode: automatic
    modifiers:
      - +1 --> e_1_2

  - type: pool
    id: p2
    activationMode: automatic

