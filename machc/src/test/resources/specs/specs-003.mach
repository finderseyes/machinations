/**
Test aspects:
- if-then(-else) statement
*/

graph main {
    function f0(v) {
        if (v > 0)
            return true;
        else
            return false;
    }

    function f1(v) {
        if (v > 0)
            return true;

        return false;
    }

    function f2() {
        let x = 10;
        {
            let y = 100;
            return x + y;
        }
        return x;
    }
}