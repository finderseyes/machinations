/**
    Test aspects:
        - Resource declaration as graph.
*/

/* solder resource */
graph soldier {
    node
        health = 10,
        weapon = 1
        ;

    let
        is_stealthy = true,
        fire_power = 10
        ;
}

graph main {
    node barrack = 10 soldier;
}