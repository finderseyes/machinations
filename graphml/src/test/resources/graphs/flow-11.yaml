# Source tests
---
nodes:
  - type: source
    id: p0
    connections:
      - --> p1

  - type: pool
    activationMode: automatic
    id: p1
    connections:
      - --> p2

  - type: drain
    id: p2
    activationMode: automatic
